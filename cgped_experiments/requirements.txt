# Core experiment dependencies.
torch>=2.1,<3
numpy>=1.24,<3
matplotlib>=3.7,<4

# Only needed to regenerate the paper from the results JSON
# (experiments/make_paper.py). Not required to run the experiments.
python-docx>=1.1
